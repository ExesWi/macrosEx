# Macros - Приложение для записи и воспроизведения макросов

Приложение для автоматизации работы с компьютером: записывает все действия пользователя (движения мыши, клики, нажатия клавиш) и может воспроизводить их в любое время. Поддерживает работу с любыми приложениями Windows, включая Excel, браузеры и другие программы.

## 🎯 Основные возможности

- **Глобальная запись действий** - записывает все действия, даже когда приложение не в фокусе
- **Воспроизведение макросов** - точное повторение записанных действий
- **Управление макросами** - создание, сохранение, загрузка и организация макросов в папках
- **Настройка записи** - выбор типов действий для записи (клавиатура, мышь, движения курсора)
- **Циклическое воспроизведение** - настройка повторения части макроса
- **Формат .amc** - сохранение макросов в XML формате с метаданными
- **Система лицензирования** - пробный период 7 дней, различные типы лицензий

## 📋 Требования

- **ОС:** Windows 10/11
- **Java:** JDK 8 или выше
- **Библиотеки:**
  - JNativeHook 2.2.2 (включена в проект)
  - Java Swing (встроена в JDK)

## 🚀 Установка и запуск

### Сборка проекта

1. Клонируйте репозиторий или распакуйте архив проекта
2. Убедитесь, что в папке `lib/` находятся следующие файлы:
   - `jnativehook-2.2.2.jar`
   - `JNativeHook-2.2.2.x86_64.dll` (для Windows x64)
3. Скомпилируйте проект:
   ```bash
   javac -cp "lib/jnativehook-2.2.2.jar" -d out/production/macros src/*.java
   ```

### Запуск приложения

```bash
java -cp "out/production/macros;lib/jnativehook-2.2.2.jar" Main
```

Или используйте готовый JAR файл:
```bash
java -jar macros.jar
```

**Важно:** При первом запуске приложение создаст скрытую папку для хранения данных лицензии:
- `C:\ProgramData\MacrosLicense\` (если есть права администратора)
- `%USERPROFILE%\MacrosLicense\` (если нет прав администратора)

## 📖 Использование

### Интерфейс приложения

Приложение имеет три основные панели:

1. **Левая панель** - дерево макросов и папок
   - Создание нового макроса (кнопка "Создать макрос")
   - Создание папки (кнопка "Создать папку")
   - Выбор макроса для редактирования/воспроизведения

2. **Центральная панель** - список действий макроса
   - Отображение всех записанных действий
   - Панель инструментов с кнопками:
     - **Начать запись** - запускает запись новых действий
     - **Остановить запись** - останавливает запись
     - **Воспроизвести** - воспроизводит выбранный макрос
     - **Очистить** - удаляет все действия текущего макроса
     - **Удалить** - удаляет выбранное действие
     - **Сохранить** - сохраняет макрос в файл .amc
     - **Загрузить** - загружает макрос из файла .amc

3. **Правая панель** - кнопка цикла
   - Настройка циклического воспроизведения части макроса

### Запись макроса

1. Нажмите кнопку **"Начать запись"**
2. В появившемся окне выберите, что записывать:
   - ✅ Записывать нажатия клавиш с клавиатуры (по умолчанию включено)
   - ✅ Записывать нажатия кнопок мыши (по умолчанию включено)
   - ✅ Записывать абсолютное перемещение курсора (по умолчанию включено)
   - ⬜ Записывать относительное перемещение курсора (по умолчанию выключено)
   - ✅ Вставлять длительные нажатия (по умолчанию включено)
3. Нажмите **"OK"**
4. Подождите 3 секунды (обратный отсчет)
5. Выполните нужные действия на компьютере
6. Остановите запись одним из способов:
   - Нажмите кнопку **"Остановить запись"** в приложении
   - Нажмите комбинацию клавиш **Shift+F11** (глобальная горячая клавиша)

### Воспроизведение макроса

1. Выберите макрос в левой панели
2. Нажмите кнопку **"Воспроизвести"**
3. Макрос начнет воспроизводиться автоматически

### Сохранение и загрузка макросов

**Сохранение:**
1. Выберите или создайте макрос
2. Нажмите кнопку **"Сохранить"**
3. Введите метаданные:
   - Описание макроса
   - Комментарий
   - Программное обеспечение (для какого ПО предназначен макрос)
4. Макрос сохранится в папку `macros/` в формате `.amc`

**Загрузка:**
1. Нажмите кнопку **"Загрузить"**
2. Выберите файл `.amc`
3. Макрос загрузится и отобразится в списке

### Циклическое воспроизведение

1. Выберите макрос
2. Нажмите кнопку **"Цикл"** (правая панель)
3. Укажите:
   - С какой строки начинать цикл
   - Сколько раз повторить
4. Нажмите **"OK"**

### Удаление действий

- **Удалить последние действия:** Меню → "Удалить последние действия" → введите количество
- **Удалить конкретное действие:** Выберите действие в списке → кнопка **"Удалить"**

## 📁 Формат файлов .amc

Макросы сохраняются в XML формате с расширением `.amc`. Структура файла:

```xml
<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<Root>
  <DefaultMacro>
    <Major/>
    <Description>Описание макроса</Description>
    <Comment>Комментарий</Comment>
    <Software>Название ПО</Software>
    <KeyDown>
      <Syntax>
        Delay 100 ms
        MoveTo 500 300
        LeftDown 500 300
        Delay 50 ms
        LeftUp 500 300
        KeyDown 67
        Delay 10 ms
        KeyUp 67
      </Syntax>
    </KeyDown>
  </DefaultMacro>
</Root>
```

### Команды в синтаксисе макроса

- `Delay X ms` - задержка в миллисекундах
- `MoveTo X Y` - перемещение курсора к координатам (X, Y)
- `LeftDown X Y` - нажатие левой кнопки мыши
- `LeftUp X Y` - отпускание левой кнопки мыши
- `RightDown X Y` - нажатие правой кнопки мыши
- `RightUp X Y` - отпускание правой кнопки мыши
- `KeyDown CODE` - нажатие клавиши (код клавиши)
- `KeyUp CODE` - отпускание клавиши
- `GoWhile X Y` - цикл: повторить Y раз, начиная со строки X

## 🔐 Система лицензирования

### Пробный период

При первом запуске приложение предоставляет **7 дней бесплатного использования**. Информация о первом запуске сохраняется в скрытом системном файле, чтобы предотвратить обход пробного периода переустановкой.

### Типы лицензий

- **Пробный период (TRIAL)** - 7 дней бесплатно
- **Тестовая (TEST_5SEC)** - 5 секунд (для тестирования)
- **Месячная (MONTHLY)** - 30 дней
- **Годовая (YEARLY)** - 365 дней
- **Бессрочная (LIFETIME)** - без ограничений

### Активация лицензии

1. Откройте меню **"Справка"** → **"Лицензия"**
2. Введите лицензионный ключ
3. Нажмите **"Активировать"**
4. Или перейдите на сайт: [macros.github.io](https://macros.github.io)

### Генерация ключей лицензии

В проекте есть утилита `LicenseKeyGenerator.java` для генерации тестовых ключей:

```bash
java -cp "out/production/macros" LicenseKeyGenerator
```

Программа предложит выбрать тип лицензии:
1. Пробный период
2. Тестовая (5 секунд)
3. Месячная
4. Годовая
5. Бессрочная

После выбора будет сгенерирован лицензионный ключ в формате: `XXXX-XXXX-XXXX-XXXX-XXXX`

**Важно:** Ключи, сгенерированные этой утилитой, предназначены только для тестирования. Для продакшена используйте серверную генерацию ключей.

### Хранение данных лицензии

Данные лицензии хранятся в скрытой папке:
- `C:\ProgramData\MacrosLicense\` (системная папка, требует прав администратора)
- `%USERPROFILE%\MacrosLicense\` (папка пользователя, если нет прав)

Файлы:
- `license.dat` - лицензионный ключ и дата активации
- `system.dat` - дата первого запуска (для пробного периода)

## 🛠️ Технические детали

### Архитектура проекта

```
src/
├── Main.java                 # Главный класс, GUI приложения
├── MacroRecorder.java        # Запись действий пользователя
├── MacroPlayer.java          # Воспроизведение макросов
├── MacroManager.java         # Управление макросами
├── MacroFileManager.java     # Работа с файлами .amc
├── Action.java               # Класс действия (клик, нажатие клавиши и т.д.)
├── NativeHookAdapter.java    # Адаптер для JNativeHook (глобальные события)
├── RecordSettings.java       # Настройки записи
├── LicenseManager.java       # Управление лицензиями
├── LicenseKeyGenerator.java # Генератор лицензионных ключей
└── AppLauncher.java          # Запуск внешних приложений
```

### Используемые технологии

- **Java Swing** - графический интерфейс
- **JNativeHook 2.2.2** - глобальный перехват событий клавиатуры и мыши
- **Java AWT Robot** - симуляция действий для воспроизведения
- **XML (DOM)** - парсинг и сохранение файлов .amc
- **Java Properties** - хранение данных лицензии

### Глобальные горячие клавиши

- **Shift+F11** - остановка записи макроса (работает глобально, даже когда приложение не в фокусе)

### Особенности реализации

1. **Глобальная запись:** Использует JNativeHook для перехвата событий на уровне системы, а не только в окне приложения
2. **Плавное движение мыши:** При воспроизведении генерируются промежуточные точки для плавного движения курсора
3. **Обработка комбинаций клавиш:** Корректная запись и воспроизведение Ctrl+C, Ctrl+V, Alt+Tab и других комбинаций
4. **Потокобезопасность:** Использование `CopyOnWriteArrayList` и `volatile` для безопасной работы с несколькими потоками
5. **Обработка ошибок:** Graceful degradation при отсутствии JNativeHook или проблем с правами доступа

## 📝 Структура папок

```
macros/
├── lib/                      # Библиотеки
│   ├── jnativehook-2.2.2.jar
│   └── JNativeHook-2.2.2.x86_64.dll
├── src/                      # Исходный код
│   ├── *.java
│   └── images/
├── macros/                   # Сохраненные макросы (.amc файлы)
│   ├── *.amc
│   └── [папки с макросами]/
└── out/                      # Скомпилированные классы
```

## ⚠️ Известные ограничения

1. **Права администратора:** Для записи глобальных событий может потребоваться запуск с правами администратора (в зависимости от настроек Windows)
2. **Безопасность:** Приложение имеет доступ ко всем действиям пользователя - используйте только из доверенных источников
3. **Совместимость:** Протестировано на Windows 10/11. Для других ОС может потребоваться дополнительная настройка JNativeHook

## 🐛 Решение проблем

### Запись не начинается

- Убедитесь, что JNativeHook DLL файл находится в папке `lib/`
- Попробуйте запустить приложение с правами администратора
- Проверьте, что нет других приложений, использующих глобальные хуки

### Макрос воспроизводится некорректно

- Убедитесь, что окна приложений находятся в тех же позициях, что и при записи
- Проверьте, что разрешение экрана не изменилось
- Увеличьте задержки в настройках записи

### Проблемы с лицензией

- Проверьте права доступа к папке `C:\ProgramData\` или `%USERPROFILE%\`
- Убедитесь, что файлы лицензии не заблокированы антивирусом
- Попробуйте запустить с правами администратора

## 📞 Поддержка

- **Сайт:** [macros.github.io](https://macros.github.io)
- **Лицензирование:** Меню "Справка" → "Лицензия"

## 📄 Лицензия

Это коммерческое программное обеспечение. Использование без действующей лицензии ограничено 7-дневным пробным периодом.

---

**Версия:** 1.0  
**Дата:** 2025  
**Разработчик:** Macros Team
